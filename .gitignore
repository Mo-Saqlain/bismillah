# ── OS / IDE ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/
.idea/
*.iml
*.ipr
*.iws
.history
*~

# ── Flutter / Dart / Pub ────────────────────────────────────────────────
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
coverage/
*.log
*.class
*.pyc
migrate_working_dir/
lib/generated_plugin_registrant.dart
app.*.symbols
app.*.map.json

# ── Android build outputs (generated) ───────────────────────────────────
/android/.gradle/
/android/captures/
/android/gradlew
/android/gradlew.bat
/android/local.properties
/android/**/GeneratedPluginRegistrant.java
/android/app/debug/
/android/app/profile/
/android/app/release/

# ── Secrets: release signing key + cloud credentials — NEVER commit ─────
secrets/
!secrets/dart_defines.example.json
android/key.properties
*.jks
*.keystore
**/secrets.dart
.env
.env.local
.env.*.local

# ── Signed / built app binaries (kept locally, e.g. _SIGNED_APK/) ───────
_SIGNED_APK/
*.apk
*.aab
*.apks

# ── Local DB / backup artefacts a user may drop at the root ─────────────
solo_con_*.db
*.before_import
*.before_rollback
# Personal backup manifest — contains signing password + cloud key in plaintext.
BACKUP_README.txt

# ── Personal scratch / AI tool caches ───────────────────────────────────
test_cases_urdu.txt
.claude/local/
**/.claude.local.json
